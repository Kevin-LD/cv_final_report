\subsection{Conclusion}
In this task, we utilize COLMAP and 2D Gaussian Splatting (2DGS) to reconstruct Object A and the background scene, leveraging multi-view geometric consistency for accurate structure recovery and high-fidelity rendering. 

For Object B and Object C, we employ generative-based 3D modeling frameworks, namely threestudio and Magic123, which enable efficient optimization of object geometry and appearance from sparse or single-view inputs.

We further analyze the training dynamics by comparing loss curves across different models, and evaluate reconstruction quality using standard metrics, including SSIM, PSNR, and LPIPS and training time.

Finally, we integrate all reconstructed components into a unified scene by fusing geometry and appearance representations. The combined scene is rendered to produce final visualizations, demonstrating consistent spatial alignment and visually coherent composition across different modeling pipelines.